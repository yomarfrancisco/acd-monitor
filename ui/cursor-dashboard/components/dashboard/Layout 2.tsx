// Legacy Layout.tsx neutralized - no longer used
// This prevents diagnostic useEffect from running and causing conflicts

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}