# ACD Monitor Backend Environment Variables

# Exchange Configuration
EX_BINANCE_SYMBOL=BTCUSDT
EX_BINANCE_TF=5m

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging
LOG_LEVEL=INFO

# CORS Origins (comma-separated)
CORS_ORIGINS=*

# Database (if needed in future)
# DATABASE_URL=postgresql://user:password@localhost/acd_monitor

# External API Keys (if needed in future)
# BINANCE_API_KEY=your_api_key_here
# BINANCE_SECRET_KEY=your_secret_key_here
